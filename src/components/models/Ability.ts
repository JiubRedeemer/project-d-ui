// eslint-disable-next-line @typescript-eslint/no-unused-vars
interface Ability {
    name: string;
    code: string;
    roomId: string;
    defaultValue: number;
    modifierValue: number;
    byCoinsValue: number;
}
